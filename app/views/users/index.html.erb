<h1 id="user-index">Users</h1>

<table class="table">
  <tr>
    <th>Name</th>
    <th></th>
  </tr>

  <% @users.each do |user| %>
    <tr>
      <td><%= link_to user.name, user_path(user) %></td>
    </tr>
  <% end %>
</table>
